# DOFree v3 environment
# Server-only TMDB bearer token. Use TMDB Read Access Token, not public frontend env.
TMDB_ACCESS_TOKEN=

# Optional Supabase values for future admin module.
# Do not add Supabase code to public pages until the admin module is ready.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
